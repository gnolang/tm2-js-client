---
"@gnolang/tm2-js-client": minor
---

Declare `getTransaction` on the `Provider` interface so `Provider`-typed callers can fetch committed transactions (previously only available on `BaseTm2Provider`).
